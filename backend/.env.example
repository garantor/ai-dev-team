# Server Configuration
PORT=3000

# PostgreSQL Database Configuration
DATABASE_URL="postgresql://user:password@host:port/database_name"
TEST_DATABASE_URL="postgresql://test_user:test_password@test_host:test_port/test_database"

# JWT Configuration for Backend Session Tokens
JWT_SECRET="your_super_secret_jwt_key_here"
JWT_EXPIRES_IN="1h"

# Firebase Admin SDK Configuration
# This should be the JSON content of your Firebase service account key file.
# You can either paste the entire JSON string here (ensure it's on one line and properly escaped if needed),
# or, for better security and deployment, store it as a base64 encoded string.
# Example (JSON string):
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type": "service_account", "project_id": "your-project-id", "private_key_id": "...", "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n", "client_email": "firebase-adminsdk-...@your-project-id.iam.gserviceaccount.com", "client_id": "...", "auth_uri": "https://accounts.google.com/o/oauth2/auth", "token_uri": "https://oauth2.googleapis.com/token", "auth_provider_x509_cert_url": "https://www.googleapis.com/oau...

# Firebase Web API Key (for client-side REST API calls, used here for server-side login verification)
# Find this in your Firebase project settings -> General -> Web API Key
FIREBASE_WEB_API_KEY="your_firebase_web_api_key_here"