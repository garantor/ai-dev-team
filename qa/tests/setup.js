// tests/setup.js
import { beforeAll, afterAll, beforeEach, afterEach } from 'vitest';

/**
 * Global setup for unit and integration tests.
 * You can add database connections, mock stores, or global setup here.
 */

beforeAll(() => {
    // Logic to run before all tests
});

afterAll(() => {
    // Logic to run after all tests
});

beforeEach(() => {
    // Logic to run before each test
});

afterEach(() => {
    // Logic to run after each test
});
